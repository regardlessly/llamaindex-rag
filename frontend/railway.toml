[build]
builder = "nixpacks"
buildCommand = "npm ci && npm run build"

[deploy]
# Railway's nixpacks static buildpack serves the dist/ folder via nginx
startCommand = "nginx -g 'daemon off;'"
healthcheckPath = "/"
healthcheckTimeout = 30
